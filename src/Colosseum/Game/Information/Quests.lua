RegInit(function()
    Colosseum_CreateQuests()
end)

function Colosseum_CreateQuests()
    -- local quest = CreateQuest()
    
    -- QuestSetTitle(quest, "Defeat the Bandits")
    -- QuestSetDescription(quest, "Eliminate all the bandits in the area to secure the region.")
    -- QuestSetDiscovered(quest, true)
    -- --QuestSetRequired(quest, true)
    -- QuestSetEnabled(quest, true)
    
    -- local questItem = QuestCreateItem(quest)
    -- QuestItemSetDescription(questItem, "Defeat all bandit leaders.")
end